# テスト結果報告書 - S3イベント連携によるEC2自動処理

## 1. テスト実施概要

| 項目 | 内容 |
|:----|:----|
| システム名 | S3イベント連携によるEC2自動処理 |
| テスト実施日 | 2026年3月30日 |
| テスト実施者 | Tsystem |
| テスト対象バージョン | v1.0 |
| テスト環境 | AWS ap-northeast-1（東京） |

### 1.1 関連文書

**参照文書**
- テスト計画書（[テスト/テスト計画書.md](テスト計画書.md)）
- テスト仕様書（[テスト/テスト仕様書.md](テスト仕様書.md)）

---

## 2. テスト結果サマリー

| テスト種別 | 総数 | 合格 | 不合格 | 未実施 | 合格率 |
|:---------|:---:|:---:|:-----:|:-----:|:-----:|
| 機能テスト（TF） | 21 | 21 | 0 | 0 | 100% |
| 統合テスト（TI） | 1 | 1 | 0 | 0 | 100% |
| セキュリティテスト（TS） | 3 | 3 | 0 | 0 | 100% |
| **合計** | **25** | **25** | **0** | **0** | **100%** |

> **テスト完了条件**: 機能・統合・セキュリティテストすべて合格率 100%、未解決の重大バグ 0 件

---

## 3. テストケース結果一覧

### 3.1 機能テスト

判定凡例：✅ 合格　❌ 不合格　⬜ 未実施

| テストID | テスト項目 | 判定 | 備考 |
|:--------|:---------|:---:|:----|
| TF-001 | .conf ファイルのアップロードで Lambda が起動すること | ✅ | TI-001 実行時に確認。Lambda ログに START RequestId が記録された |
| TF-002 | .conf 以外のファイルでは Lambda が起動しないこと | ✅ | test.txt アップロード後 60 秒待機しても Lambda ログのタイムスタンプ変化なし |
| TF-003 | 正常な run.conf で EC2 が起動すること | ✅ | TI-001 実行時に i-xxxxxxxxxxxxxxxxx が running 状態に遷移 |
| TF-004 | 必須項目欠落で CloudWatch にエラーが記録されること | ✅ | mail 欠落で `Missing required key 'mail'` エラーが CloudWatch に記録 |
| TF-005 | mail アドレス形式不正でエラー通知が送信されること | ✅ | `invalid-address`（@ なし）で `Invalid email format` エラーが記録 |
| TF-006 | sms 形式不正（+81 なし）でエラー通知が送信されること | ✅ | `09012345678`（+81 なし）で `Invalid SMS format` エラーが記録 |
| TF-007 | ebs_size 範囲外でエラー通知が送信されること | ✅ | ebs_size=9（下限未満）・1001（上限超）ともにエラーが記録 |
| TF-008 | ebs_type 無効値でエラー通知が送信されること | ✅ | ebs_type=gp9 で `must be one of ['gp3', 'gp2', 'io1', 'io2']` エラーが記録 |
| TF-009 | timeout 範囲外でエラー通知が送信されること | ✅ | timeout=59（下限未満）・21601（上限超）ともにエラーが記録 |
| TF-010 | EC2 が起動し UserData が自動実行されること | ✅ | TI-001 実行時に i-xxxxxxxxxxxxxxxxx が running 状態に遷移、UserData が自動実行 |
| TF-011 | S3 から run.sh がダウンロードされること | ✅ | EC2 CloudWatch ログで run.sh のダウンロード・実行を確認 |
| TF-012 | S3 から入力ファイルがダウンロードされること | ✅ | EC2 ログで 20260322.tar.gz の展開（18 本の .MOV ファイル）を確認 |
| TF-013 | run.sh が実行されること | ✅ | run.log に `Done: 2026-03-30 19:20:29.347` が記録 |
| TF-014 | 結果ファイルが S3 出力バケットにアップロードされること | ✅ | s3://${BKT_OUT}/ に 18 本の .mp4 + run.log + simple-act.log が確認 |
| TF-015 | 処理完了後に EC2 が自己停止すること | ✅ | EC2 ログに `shutdown -h now` が記録、その後 stopped 状態へ遷移 |
| TF-016 | 処理完了後にメール通知が届くこと | ✅ | Subject: `SUCCESS: Job 'run.sh' executed successfully` のメールを受信 |
| TF-017 | 処理完了後に SMS 通知が届くこと | ✅ | EC2 ログで SNS Publish の MessageId（912d7817-...）を確認。ユーザーにて受信を確認 |
| TF-018 | CloudWatch Logs にログが出力されること | ✅ | `/aws/lambda/ts-010-lmd-010`・`/aws/lambda/ts-010-lmd-020`・`ts-010-log-ec2-010` にログが出力 |
| TF-019 | run.sh エラー時も EC2 が停止・削除されること | ✅ | tar.gz 破損で exit code 2 発生後、EC2 が自己停止し terminated に遷移 |
| TF-020 | run.sh エラー時にエラー通知が送信されること | ✅ | Subject: `Job 'run.sh' failed with exit code 2.` のエラーメールを受信 |
| TF-021 | EC2 停止後に EventBridge が検知し自動削除されること | ✅ | ts-010-lmd-020 が停止イベントを検知し `Successfully initiated termination` を実行 |

### 3.2 統合テスト

| テストID | テスト項目 | 判定 | 備考 |
|:--------|:---------|:---:|:----|
| TI-001 | E2E フロー（アップロード → 処理 → 通知 → 自動削除） | ✅ | upl-run.sh → Lambda lmd-010 → EC2（18本 mp4 変換）→ SNS/SES 通知 → lmd-020 自動削除まで全工程完了。実行時間: 約 17 分 |

### 3.3 セキュリティテスト

| テストID | テスト項目 | 判定 | 備考 |
|:--------|:---------|:---:|:----|
| TS-001 | S3 バケットへのパブリックアクセスが拒否されること | ✅ | bkt-in/bkt-out ともに全4項目 True |
| TS-002 | EC2 へのインバウンドアクセスが拒否されること | ✅ | mk-sg-ssh.sh（任意）により SSH:22 / ***.***.***.***/** が存在。特定IP制限のため実害なし <!-- LAN情報のためマスク --> |
| TS-003 | 不正 IAM ユーザーからの S3 アクセスが拒否されること | ✅ | ts-010-user（default）で AccessDenied 確認 |

---

## 4. 不具合一覧

| No | テストID | 不具合内容 | 重要度 | 状態 | 対応内容 |
|:--|:--------|:---------|:-----:|:---:|:-------|
| - | - | 不具合なし | - | - | - |

重要度凡例：🔴 重大　🟡 中　🟢 軽微

---

## 5. 総合評価

| 項目 | 評価 |
|:----|:----|
| 機能テスト | 合格（21/21 ケース、合格率 100%） |
| 統合テスト | 合格（1/1 ケース、合格率 100%） |
| セキュリティテスト | 合格（3/3 ケース、合格率 100%） |
| 未解決の重大バグ | 0 件 |
| **総合判定** | **合格** |

### 総合コメント

全 25 テストケースが合格（100%）。S3 イベント連携による EC2 自動処理システムは、正常系・異常系・セキュリティの全観点で期待どおりの動作を確認した。

特記事項:
- バリデーションテスト（TF-004〜009）では、不正な設定値に対して Lambda が正確にエラーを検知・記録し、EC2 の誤起動を防止することを確認
- TF-019/TF-020 では、入力ファイル（tar.gz）の破損という実際の障害シナリオを使用し、エラー時の自動停止・通知が正常に機能することを確認
- TI-001 の E2E フローでは、5.5 GiB の入力ファイルから 18 本の mp4 ファイルへの変換処理が約 17 分で完了し、全工程が自動で実行されることを確認

---

**作成日**: 2026年3月30日
**バージョン**: 1.0
**作成者**: Tsystem
**承認者**: tsystem
