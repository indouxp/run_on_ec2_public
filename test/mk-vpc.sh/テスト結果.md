# テスト結果

## 対象スクリプト
`src/shell/mk-vpc.sh`

## 実施日
2026-03-13

## テスト環境
- 実行ユーザー（tc-010〜020）: デフォルト認証情報（AWS呼び出し前に失敗するため不要）
- 実行ユーザー（tc-030）: デフォルト認証情報
- 実行ユーザー（tc-040〜060）: `ts-010-role-build`（AssumeRole）
- 実行ユーザー（tc-070）: `ts-010-user`（デフォルト）

## 備考
- `mk-vpc.sh` はVPCが既存の場合、削除して再作成する。
- tc-040 の前処理では依存リソース（IGW・サブネット・非デフォルトSG）を先にクリーンアップしてから実行する。
- tc-060 の検証で `ec2:DescribeVpcAttribute` が必要なため、`ts-010-role-build-policy` に追加済み（`mk-role-build.sh` も同様に修正済み）。

## テスト結果一覧

| TC | 内容 | 期待RC | 実際RC | 結果 |
|----|------|--------|--------|------|
| tc-010 | config.sh が存在しない場合の異常終了 | 1 | 1 | PASS |
| tc-020 | LOG_PATH が存在しないディレクトリの場合の異常終了 | 1 | 1 | PASS |
| tc-030 | MY_SRC_DIR を作成できない場合の異常終了 | 1 | 1 | PASS |
| tc-040 | VPCが存在する状態から実行、正常処理（削除→再作成） | 0 | 0 | PASS |
| tc-050 | VPCが存在しない状態から実行、正常処理（新規作成） | 0 | 0 | PASS |
| tc-060 | 作成されたVPCの設定検証 | 0 | 0 | PASS |
| tc-070 | EC2権限が不足している状態での異常終了 | 非0 | 254 | PASS |

## tc-060 検証内容

| 検証項目 | 期待値 | 結果 |
|---------|--------|------|
| VPCが存在すること | VpcId が取得できる | OK |
| CIDRブロック | 10.0.0.0/16 | OK |
| DNS ホスト名 | 有効（True） | OK |
| DNS 解決 | 有効（True） | OK |

## tc-070 エラー内容
```
UnauthorizedOperation: ts-010-user is not authorized to perform: ec2:DescribeVpcs
```
